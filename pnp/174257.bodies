class sample_histogram
!!!313009.cpp!!!	insert_value(in val : sample_histogram::float_type) : void
// ignore out of range
if (this->axis_definition_->in_range(val))
{
  this->bincounter_[this->axis_definition_->bin_number (val)] += 1.0;
}

