class init_helper
!!!1519919.cpp!!!	init_helper()
    if (0 == counter)
      {
	const std::string description("The temperature of the simulation in Kelvin\n  (Valid values 260 < " + constants::fstsi() + " < 380)");
	utility::input_help::exemplar().desc(constants::fschnl()).add_options()
	  (constants::fstsi().c_str(), description.c_str());
      }
    ++counter;
