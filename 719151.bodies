class compound_choice
!!!1696687.cpp!!!	do_generate(in result : , in rgen : random_distribution) : void
for (auto parts = this->subparts.begin (); parts != this->subparts.end (); ++parts)
  {
    parts->do_generate (result, rgen);
    if (not result.is_valid ()) break;
  }
!!!620209.cpp!!!	report(in sim : simulator, inout log : ostream, in att : size_t) : void
this->base_choice::report(sim, log);
for (auto const& chc : this->children_)
{
  chc.report(sim, log);
}
