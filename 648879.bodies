class ValidatorRegion
!!!1131567.cpp!!!	valid_position(in a_pt : ) : bool
return this->in_region (a_z, a_r) and
         this->validator_->valid_position (a_z, a_r, this->sphere_radius_);

!!!1131567.python!!!	valid_position(in a_pt : ) : bool
return self.in_region (a_pt) and self.validator (a_pt, self.sphere_radius)

